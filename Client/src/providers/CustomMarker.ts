export class CustomMarker {
    id:number
    lat: number;
    lng: number;

    constructor(id:number, lat: number, lng: number) {
        this.id = id;
        this.lat = lat;
        this.lng = lng;
    }
}
